[Package]
name          = "yaml"
version       = "0.1.0"
author        = "Christoph Herzog"
description   = "Robust YAML parser."
license       = "MIT"

srcDir        = "src"

[Deps]
Requires: "nim >= 0.12.0, utils >= 0.1, values >= 0.1"
